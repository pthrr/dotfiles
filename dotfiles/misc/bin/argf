#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 2 ]]; then
    echo "Usage: $0 <file> <cmd> [args...]" >&2
    exit 1
fi

FILE="$1"
shift

mapfile -t lines < "$FILE"

for f in "${lines[@]}"; do
    echo "Executing ${f} .."
    "$@" "$f"
done
