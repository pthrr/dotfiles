[project]
name = "drums-to-gp-drumsep-env"
version = "0.1.0"
requires-python = ">=3.10,<3.15"
dependencies = [
    "mdxnet-infer==0.1.0",
    "torch>=2.0",
]

[tool.uv.sources]
torch = { index = "pytorch-cpu" }

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true
