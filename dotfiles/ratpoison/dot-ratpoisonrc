set startupmessage 0
set border 0
set padding 0 16 0 0
set winname class
exec rpws init 4 -k
exec nm-applet
exec polybar top
unmanage panel
unmanage polybar
bind c exec $TERMINAL
bind f exec $BROWSER
bind g exec spacefm
bind d exec claws-mail
