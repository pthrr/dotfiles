---
description: >-
  Hot paths — measurement, memory traffic, branches, parallelism, numerics. Enable
  when profiling, kernels, or performance-sensitive code.
alwaysApply: false
globs: "**/*.{c,h,cc,cpp,cxx,hpp,hxx,rs,go,py,pyi,ts,tsx,js,jsx,mjs,cjs,java,kt,kts,cs,swift,zig,v,sv,vhd,hs,ml,fs,erl,ex,exs,lua,rb,php,asm,s,S}"
---

# Performance

- **No claim without numbers**; **no optimization without profile** (wall + counters when unclear).
- **Hot loop owns memory layout and allocation** nearby; do not abstract over unknown allocation on hot paths.
- Lay out hot data for **sequential access**; **SoA** when fields have different access rates.
- **False sharing** will not appear as a named line in a profile — **pad or partition** independent hot writer lines.
- **Serial efficiency before scaling** cores out.
- **Floating-point:** default **bit-reproducible** ordering; **fast-math / reorder** only with explicit team-visible opt-in and note.
- **Integer overflow / narrowing:** explicit policy on security- or money-critical paths (checked, wider type, or proof).

## Cross-rule

- Determinism / no heap after init: `embedded.mdc` — baseline: `orthodox.mdc`
