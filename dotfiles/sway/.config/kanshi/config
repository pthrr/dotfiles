# ==============================================================================
# KANSHI CONFIGURATION - Automatic screen management
# ==============================================================================
# Kanshi automatically detects connected displays and applies matching profiles.
# When screens are plugged/unplugged, kanshi switches profiles automatically.
#
# Get your output names and modes: swaymsg -t get_outputs
#
# Profiles are matched top-to-bottom. First matching profile is applied.
# A profile matches if ALL specified outputs are connected.

# Laptop + External monitor (docked)
# This will match when both laptop screen and any external display are connected
profile docked {
    output eDP-* enable scale 1.5
    output * enable position 2560,0
}

# Laptop only - HiDPI screen
# Matches laptop display patterns (eDP-1, eDP-2, etc)
profile laptop_hidpi {
    output eDP-* enable scale 1.5
}

# Desktop with external monitor only
# Matches when external display is connected but laptop screen isn't
profile external_only {
    output * enable
}

# Fallback - matches any output configuration
profile default {
    output * enable
}
