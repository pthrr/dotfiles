#!/usr/bin/env bash
#set -eu -o pipefail

BAR_FIFO=/tmp/bar_fifo

if [ ! -e "${BAR_FIFO}" ] ; then
    exit 1
fi

datetime()
{
    DATE=$(date '+%a, KW %V, %d.%m.%y, %H:%M')
}

while : ; do
    datetime
    echo -e "DATE|${DATE}"
    sleep 30
done > ${BAR_FIFO}
