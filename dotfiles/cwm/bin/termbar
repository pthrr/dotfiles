#!/usr/bin/env bash

today()
{
    DATE=$(date '+%a, KW %V, %d.%m.%y - %H:%M')
}

desktop_id()
{
    DESKTOP_ID=$(xprop -root 32c '\t$0' _NET_CURRENT_DESKTOP | cut -f 2)
}

while true ; do
    today
    desktop_id
    echo "[${DESKTOP_ID}] ${DATE} | "
    sleep 1
done
